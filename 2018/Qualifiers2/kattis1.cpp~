#include <iostream>
#include <cstdlib>
#include <sstream>
using namespace std;
int main() {
  int count;
  cin >> count;
  int num[4][3] = {};
  int n = 1;
  for(int i = 0; i < 3; i++) {
    for(int j = 0; j < 3; j++) {
      num[i][j] = n;
      n++;
    }
  }
  num[3][0] = 0;
  for(int i = 0; i < count; i++) {
    int k;
    cin >> k;
    istringstream iss(k);
    
    
